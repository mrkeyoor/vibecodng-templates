// Source slug: cultui-feature-carousel
// Author repo: https://github.com/nolly-studio/cult-ui
import{useState}from'react';import Phone from'./Phone.jsx';const slides=[['today','Make today feel doable','See just what matters now, with satisfying taps and enough whitespace to breathe.'],['garden','Progress you can feel','Your garden reflects consistency over time-more seasonal landscape than scoreboard.'],['reflect','Close the loop softly','A ten-second check-in turns routines into useful self-knowledge, one honest note at a time.']];export default function Screenshots(){const[active,setActive]=useState(0);return <section id="screens" className="section screenshots"><div className="shell"><div className="grid gap-8 lg:grid-cols-2 lg:items-end"><div data-reveal><p className="eyebrow">Inside the app</p><h2 className="section-title mt-4">A little pocket<br/><span>of momentum.</span></h2></div><div className="slide-copy" aria-live="polite" data-reveal><p className="slide-count">0{active+1} / 03</p><h3>{slides[active][1]}</h3><p>{slides[active][2]}</p><div className="slide-controls"><button type="button" aria-label="Previous screenshot" onClick={()=>setActive((active+2)%3)}>←</button><button type="button" aria-label="Next screenshot" onClick={()=>setActive((active+1)%3)}>→</button></div></div></div><div className="carousel-stage mt-12" data-reveal>{slides.map(([screen],i)=><div key={screen} className={`phone-slide ${i===active?'active':''}`} aria-hidden={i!==active}><Phone screen={screen}/></div>)}</div></div></section>}
