import { useState } from 'react'
// Source slug: originui-comp-107
// Author repo: https://github.com/origin-space/originui
const snippets={npm:'npm install @gridnote/sdk',pnpm:'pnpm add @gridnote/sdk',bun:'bun add @gridnote/sdk'}
export default function Hero(){const[tab,setTab]=useState('npm');const[copied,setCopied]=useState(false);const copy=()=>{navigator.clipboard?.writeText(snippets[tab]);setCopied(true);setTimeout(()=>setCopied(false),1200)};return <section id="top" className="doc-hero" data-reveal-group><div className="breadcrumb" data-reveal>DOCS <span>/</span> INTRODUCTION</div><div className="hero-badge" data-reveal><span>NEW</span> Gridnote 2.4 is live <i>→</i></div><h1 data-reveal style={{'--order':1}}>Ship knowledge<br/>that stays <em>in sync.</em></h1><p data-reveal style={{'--order':2}}>Gridnote is a structured knowledge layer for product teams. Write once, connect your sources, and keep every surface current-from internal docs to customer-facing changelogs.</p><div className="hero-actions" data-reveal style={{'--order':3}}><a className="primary" href="#quickstart">Start building <span>→</span></a><a href="#concepts">How it works</a></div><div className="install" data-reveal style={{'--order':4}}><div className="tabs" role="tablist" aria-label="Package manager">{Object.keys(snippets).map(x=><button key={x} role="tab" aria-selected={tab===x} onClick={()=>setTab(x)}>{x}</button>)}</div><div className="command"><code><span>$</span> {snippets[tab]}</code><button onClick={copy} aria-label="Copy install command">{copied?'Copied':'Copy'}</button></div></div></section>}
