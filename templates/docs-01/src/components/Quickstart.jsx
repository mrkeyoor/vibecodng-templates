// Source slug: magicui-file-tree
// Author repo: https://github.com/magicuidesign/magicui
export default function Quickstart(){return <section id="quickstart" className="doc-section" data-reveal-group><div className="section-label" data-reveal>01 - QUICKSTART</div><h2 data-reveal>From zero to a live collection.</h2><p className="lede" data-reveal>Create a workspace, define your content shape, and publish your first note in under five minutes.</p><div className="quick-grid"><ol className="steps" data-reveal>{[['Install the SDK','Add the tiny client to your app.'],['Create a collection','Define fields once; Gridnote validates every entry.'],['Publish everywhere','Use one query for product, docs, and updates.']].map(([t,p],i)=><li key={t}><span>0{i+1}</span><div><strong>{t}</strong><p>{p}</p></div></li>)}</ol><div className="code-card" data-reveal style={{'--order':1}}><div className="code-top"><span>project</span><i>•••</i></div><div className="file-tree"><p>▾ <b>gridnote-demo</b></p><p className="indent">▾ <b>src</b></p><p className="indent2 active">◇ content.js</p><p className="indent2">◇ app.jsx</p><p className="indent">□ gridnote.config.js</p></div><pre><code><span>import</span> {'{ createClient }'} <span>from</span> <i>&apos;@gridnote/sdk&apos;</i>{'\n\n'}<b>const</b> gridnote = createClient({'{'}{'\n  '}workspace: <i>&apos;atlas&apos;</i>{'\n'}{'}'}){'\n\n'}<b>const</b> notes = <span>await</span> gridnote{'\n  '}.collection(<i>&apos;updates&apos;</i>){'\n  '}.published()</code></pre></div></div></section>}
